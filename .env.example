NEBIUS_API_KEY=XXX

# Optional. If empty, the DAG still writes local metrics and manifest files.
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_EXPERIMENT=coding-agent-evals

# Optional long-term artifact URI, e.g. s3://bucket/prefix/<run-id>/.
ARTIFACT_URI=
